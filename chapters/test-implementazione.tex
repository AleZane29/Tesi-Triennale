\chapter{Implementazione dei test automatici}
\label{cap:test-implementazione}

\intro{Il presente capitolo descrive l'introduzione di una strategia di testing
automatico nel framework \emph{Sia.Server.Framework}, fino a quel momento privo di
qualsiasi struttura di test versionata. Vengono illustrate le quattro tipologie di
test adottate, le scelte tecnologiche, i vantaggi concreti ottenuti e i difetti
latenti emersi grazie ai test stessi.}

\section{Contesto}

Prima dell'intervento, l'unica forma di verifica disponibile era la prova manuale
dell'interfaccia. Le conseguenze pratiche erano la mancanza di una rete di sicurezza
in fase di \emph{refactor}, l'impossibilità di modificare i moduli \emph{core}
(\texttt{Sia.Base}, \texttt{Sia.Grid}, \texttt{Sia.Devextreme.Crud}) con fiducia, e
l'assenza di una \emph{baseline} oggettiva per misurare il comportamento atteso al
variare dei dati di configurazione.

L'obiettivo dell'intervento è stato duplice: impostare una convenzione di testing
replicabile su tutti i moduli del framework, e fornire una prima copertura concreta
sui moduli con maggiore superficie pubblica, comprensiva di tutte e quattro le
tipologie di test descritte nelle sezioni seguenti.

\section{Modello a quattro livelli}

La strategia adottata distingue quattro tipologie di test con responsabilità
complementari e nessuna sovrapposizione, organizzate lungo un asse che va dall'unità
più piccola di codice all'esperienza completa dell'utente.

\subsection{Unit test backend}

Gli \emph{unit test} backend esercitano il singolo controller con tutte le sue
dipendenze sostituite da \emph{mock}: il servizio di dominio non viene mai invocato
realmente, le chiamate a \emph{SignalR}, al \emph{logger} e al servizio di permessi
sono simulate. La domanda a cui rispondono è: \emph{dato un service che si comporta
in un certo modo, il controller produce la risposta corretta?}

Questo livello è veloce (l'intera suite gira in pochi secondi), deterministico e
non richiede né database né rete. Per ogni \emph{endpoint} vengono scritti almeno tre
test: uno sul ramo positivo (verifica il tipo di risposta, gli argomenti passati al
service, i \emph{side-effect} attesi come l'invio di notifiche \emph{SignalR}), uno
sul ramo di validazione (verifica che in presenza di input non valido il service non
venga mai invocato), e uno sul ramo eccezionale (verifica che il controller gestisca
correttamente le eccezioni del service).

Il modulo pilota scelto è stato \texttt{Sia.Grid}, la cui suite copre i dieci
\emph{endpoint} del \texttt{GridDataController}. Lo stesso schema è stato poi
replicato su \texttt{Sia.Devextreme.Crud}, aggiungendo la copertura del meccanismo
di impersonazione utente, specifico di quel modulo. L'esercizio ha portato alla
luce alcune asimmetrie non documentate del controller: pattern di \texttt{catch}
usati in modo non uniforme tra gli endpoint, un overload nascosto di
\texttt{InsertImages}. Ciascuna è stata annotata nei \texttt{README.md} dei
progetti di test, fornendo documentazione viva per i futuri manutentori.

\subsection{Integration test backend}

Se gli \emph{unit test} verificano la logica del controller in isolamento, gli
\emph{integration test} verificano che l'intera pipeline funzioni con il database
reale. L'host ASP.NET Core viene avviato in-process tramite
\texttt{WebApplicationFactory}, le richieste HTTP vengono eseguite contro un
\texttt{TestServer} senza aprire \emph{socket} reali, e il flusso attraversa tutti
gli strati fino alle \emph{stored procedure} su SQL Server. La domanda è:
\emph{per ogni configurazione presente in produzione, il sistema risponde correttamente?}

Questo approccio esercita ciò che gli \emph{unit test} per costruzione non possono
toccare: il \emph{routing}, il \emph{model binding}, i middleware di autenticazione,
l'esecuzione reale delle query SQL e la correttezza della configurazione
per-componente memorizzata nelle tabelle del database.

Sono stati prodotti tre livelli di \emph{integration test}. Il primo, denominato
\emph{smoke}, itera su tutti i componenti presenti nel database --- $138$ griglie
per \texttt{Sia.Grid} e $65$ componenti CRUD per \texttt{Sia.Devextreme.Crud} ---
e verifica che ciascuno restituisca una risposta valida. Il secondo livello,
\emph{lifecycle}, verifica il ciclo completo di scrittura per un sottoinsieme di
componenti: crea un record, lo legge, lo modifica, verifica la modifica, lo elimina
e verifica l'avvenuta eliminazione (con supporto sia per la cancellazione fisica sia
per quella logica, dove il record viene disattivato anziché rimosso). Il terzo
livello, specifico di \texttt{Sia.Grid}, verifica l'endpoint di \emph{summary}
(calcolo di aggregati come somma, media e conteggio) su tutte le griglie che
dichiarano colonne aggregabili nella propria configurazione.

Un episodio significativo emerso durante i \emph{lifecycle test} merita menzione:
tutti i test di scrittura fallivano con un errore SQL identico, riferito a
\emph{synonym} che puntavano a un database di audit log inesistente sul server di
sviluppo. L'audit, di fatto, non era mai stato operativo su quell'ambiente. I test
hanno reso visibile una situazione preesistente e latente; la soluzione adottata è
stata rimuovere i $316$ \emph{synonym} obsoleti, rendendo le scritture nuovamente
funzionanti.

\subsection{Unit test frontend}

Sul lato \emph{frontend}, la libreria Angular \texttt{sia-grid} è stata scelta come
modulo pilota in simmetria con il corrispettivo backend. Gli \emph{unit test} sono
scritti con \textbf{Vitest}, integrato nativamente con il toolchain Vite già in
uso nel workspace, e verificano in isolamento la logica TypeScript pura: la corretta
costruzione delle richieste HTTP negli \emph{API client}, la logica di stato e
trasformazione dei servizi, il comportamento delle \emph{directive} (ad esempio la
resistenza del ridimensionamento delle colonne), e le funzioni pure del componente
principale, come i filtri, i \emph{signal computed} e il \emph{debounce} del filtro
globale verificato con \emph{fake timer}.

Questi test non richiedono né \emph{browser} né \emph{backend} attivo, girano in
pochi secondi e sono deterministici. Ciò che non possono coprire --- il
\emph{rendering} reale del template, l'integrazione con le librerie UI di terze
parti, le interazioni utente --- è territorio dei test \emph{end-to-end}.

\subsection{Test end-to-end con Playwright}

I test \emph{end-to-end} (E2E) rappresentano il livello più alto della piramide: il
\emph{frontend} Angular e il \emph{backend} .NET sono entrambi avviati realmente, e
\textbf{Playwright} guida un'istanza di Chromium come farebbe un utente umano,
verificando che le funzionalità funzionino dall'apertura del menu fino alla risposta
del server.

Il vantaggio di questo approccio è la fedeltà assoluta alla realtà: un singolo
\emph{assert} (``la prima riga della tabella è visibile e contiene del testo'')
copre l'intera catena che va dal routing Angular alla \emph{stored procedure} SQL,
passando per l'autenticazione, la serializzazione del payload e il rendering del
DOM. Il costo è la velocità (ogni test richiede secondi, non millisecondi) e la
dipendenza dallo stato del sistema. Per questo i test E2E si concentrano sui flussi
utente strategici, lasciando la copertura della logica di dettaglio agli \emph{unit
test}.

La suite pilota copre il componente \texttt{sia-mat-grid} con nove scenari distinti.
Alcuni esempi concreti:

\begin{description}
  \item[\emph{Smoke}] Verifica che la griglia carichi correttamente: apre il menu
    \emph{Elenco Componenti}, attende che la tabella sia visibile, conta le righe e
    verifica che il paginatore mostri una \emph{label} di range valida (es.\
    ``$1 - 25$ di $342$''). Se questo test fallisce, qualsiasi altro test sarebbe
    inutile.
  \item[\emph{Sorting}] Legge la sequenza dei valori di una colonna, clicca
    sull'\emph{header} per innescare il riordino, e verifica che i valori siano
    effettivamente ordinati. Poiché \texttt{sia-mat-grid} ha una propria
    implementazione del sort, non ci si può affidare agli attributi ARIA standard:
    la verifica è comportamentale.
  \item[\emph{Filtering}] Imposta un filtro ``inizia con V'' sulla prima colonna
    filtrabile, verifica che tutti i valori visibili rispettino il criterio, poi
    rimuove il filtro e verifica che il numero di righe torni esattamente a quello
    iniziale. È un \emph{round-trip} completo che esercita sia l'applicazione sia il
    \emph{cleanup} del filtro.
  \item[\emph{Export Excel}] Registra un listener sull'evento di \emph{download} del
    browser \emph{prima} di cliccare il pulsante di esportazione (l'ordine è
    fondamentale: l'evento è sincrono rispetto al click), poi verifica che il file
    scaricato abbia estensione \texttt{.xlsx}.
  \item[\emph{Error 500}] Intercetta via \texttt{page.route} tutte le chiamate HTTP
    verso gli endpoint della griglia e le sostituisce con una risposta sintetica
    \texttt{500 Internal Server Error}, senza spegnere il \emph{backend}. Il test
    verifica che la UI non vada in \emph{crash} e che compaia una notifica di errore
    all'utente. È il test che formalizza il contratto di robustezza: nessun errore
    HTTP deve portare l'interfaccia in uno stato non recuperabile.
\end{description}

Un aspetto tecnico rilevante è la gestione dell'autenticazione: eseguire il
\emph{login} via UI prima di ogni \emph{spec} sarebbe lento e ridondante. Il pattern
adottato prevede un \texttt{globalSetup} che esegue il \emph{login} una sola volta
all'avvio della suite e salva lo stato di sessione su disco; tutti gli \emph{spec}
ereditano questo stato e partono già autenticati. Una sottigliezza scoperta
sul portale è che il \emph{token} di accesso viene memorizzato in
\texttt{sessionStorage}, che Playwright non persiste di default: la soluzione è
estrarlo manualmente al termine del \texttt{globalSetup} e re-iniettarlo tramite
uno script di inizializzazione prima di ogni navigazione.

\section{Vantaggi ottenuti}

L'impatto più immediato dell'introduzione dei test è stata la scoperta di difetti
latenti che non sarebbero emersi prima di un incidente in produzione. Iterando
automaticamente su tutti i componenti del database, sono stati trovati due punti del
codice in cui certe combinazioni di dati causavano \emph{crash} con messaggi criptici:
un \texttt{NullReferenceException} in \texttt{BaseRepository} e una gestione
difensiva mancante in \texttt{GridDataService}. Entrambi sono stati corretti prima
che il codice uscisse dall'ambiente di sviluppo.

Sul piano strategico, il beneficio più duraturo è la trasformazione del modello di
verifica da implicito (``funziona se non lo segnala nessuno'') a esplicito (``ogni
regressione che attraversa la suite viene catturata''). Modificare il framework con
fiducia, documentare il comportamento atteso in forma eseguibile, accorciare
l'\emph{onboarding} dei nuovi sviluppatori: sono conseguenze che si accumulano nel
tempo. Per garantire che questo investimento non si esaurisca con i moduli pilota, è
stata formalizzata una convenzione di scaffolding --- codificata nelle regole
dell'agente di sviluppo --- per cui ogni nuovo modulo creato nel framework nasce con
il proprio progetto di test gemello.

\section{Risultati quantitativi}

Al termine dell'intervento, il framework dispone di:

% \begin{itemize}
%   \item \textbf{$51$ unit test backend} \emph{green} distribuiti su \texttt{Sia.Grid}
%     (22) e \texttt{Sia.Devextreme.Crud} (29);
%   \item \textbf{$\sim$220$ integration test backend}: smoke su $138$ griglie e $65$
%     componenti CRUD, $12/12$ lifecycle test \emph{green} su cicli INSERT $\to$
%     UPDATE $\to$ DELETE (incluso il supporto a cancellazione logica e chiavi
%     primarie composite), $9/10$ summary test \emph{green} sulle prospettive di
%     aggregazione;
%   \item \textbf{$103$ unit test frontend} \emph{green} su \texttt{sia-grid},
%     distribuiti su otto file \emph{spec};
%   \item \textbf{9 test E2E} su \texttt{sia-mat-grid} (\emph{smoke}, \emph{sort},
%     \emph{filtering}, \emph{pagination}, \emph{resize}, \emph{export},
%     \emph{refresh}, \emph{multi-grid}, \emph{error-500}).
% \end{itemize}